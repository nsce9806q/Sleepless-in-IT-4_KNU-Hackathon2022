package sleeplessinit4.hanzip.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import sleeplessinit4.hanzip.entity.ParentMissionEntity;

public interface ParentMissionRepository extends JpaRepository<ParentMissionEntity, Long> {
}
